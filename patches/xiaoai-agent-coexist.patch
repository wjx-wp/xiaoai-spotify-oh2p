diff --git a/deploy/client-patch/patches/02-login.patch b/deploy/client-patch/patches/02-login.patch
index 2b988e2..5d43681 100644
--- a/deploy/client-patch/patches/02-login.patch
+++ b/deploy/client-patch/patches/02-login.patch
@@ -6,16 +6,6 @@
  daemon:*:0:0:99999:7:::
  ftp:*:0:0:99999:7:::
  network:*:0:0:99999:7:::
-diff --color -Naur squashfs-root/etc/inittab squashfs-root-patched/etc/inittab
---- squashfs-root/etc/inittab	2024-12-06 15:11:58
-+++ squashfs-root-patched/etc/inittab	2025-02-13 20:56:13
-@@ -1,5 +1,5 @@
- ::sysinit:/etc/init.d/rcS S boot
- ::shutdown:/etc/init.d/rcS K shutdown
- tts/0::askfirst:/bin/ash --login
--ttyS0::askfirst:/bin/login
-+ttyS0::askfirst:/bin/ash --login
- tty1::askfirst:/bin/ash --login
 diff --color -Naur squashfs-root/etc/pam.d/common-auth squashfs-root-patched/etc/pam.d/common-auth
 --- squashfs-root/etc/pam.d/common-auth	2024-12-06 15:11:58
 +++ squashfs-root-patched/etc/pam.d/common-auth	2025-02-13 21:09:02
diff --git a/deploy/client-patch/src/patch.sh b/deploy/client-patch/src/patch.sh
index 5601ab5..053d9b7 100755
--- a/deploy/client-patch/src/patch.sh
+++ b/deploy/client-patch/src/patch.sh
@@ -7,6 +7,7 @@ WORK_DIR=$BASE_DIR/temp
 
 PASSWORD=${SSH_PASSWORD:-"open-xiaoai"}
 PASSWORD=$(openssl passwd -1 -salt "openxiao" "$PASSWORD")
+XIAOAI_MODE=${XIAOAI_MODE:-"coexist"}
 
 # 应用指定目录下的补丁文件
 apply_patches() {
@@ -18,6 +19,10 @@ apply_patches() {
     if [ -d "$patch_dir" ]; then
         for file in "$patch_dir"/*; do
             if [ -f "$file" ]; then
+                if [ "$XIAOAI_MODE" = "coexist" ] && [ "$(basename "$file")" = "05-asound-mute-native.patch" ]; then
+                    echo "⏭️  共存模式：跳过 $(basename "$file")，保留原生小爱麦克风与米家语控"
+                    continue
+                fi
                 if [[ "$file" == *.patch ]]; then
                     # 创建临时文件用于占位符替换
                     local temp_patch=$(mktemp)
diff --git a/deploy/client-patch/src/extract.sh b/deploy/client-patch/src/extract.sh
index 3a73c4c..f308438 100755
--- a/deploy/client-patch/src/extract.sh
+++ b/deploy/client-patch/src/extract.sh
@@ -11,7 +11,12 @@ if [ ! -f "$BASE_DIR/assets/$FIRMWARE.bin" ]; then
     exit 1
 fi
 
-rm -rf "$WORK_DIR" && mkdir -pv "$WORK_DIR" && cd $WORK_DIR
+if [ -L "$WORK_DIR" ]; then
+    mkdir -pv "$WORK_DIR"
+else
+    rm -rf "$WORK_DIR" && mkdir -pv "$WORK_DIR"
+fi
+cd $WORK_DIR
 
 python3 $BASE_DIR/src/extract.py -e "$BASE_DIR/assets/$FIRMWARE.bin" -d "$WORK_DIR/$FIRMWARE"
 
diff --git a/deploy/client-patch/src/squashfs.sh b/deploy/client-patch/src/squashfs.sh
index 0b3e557..2a9fc43 100755
--- a/deploy/client-patch/src/squashfs.sh
+++ b/deploy/client-patch/src/squashfs.sh
@@ -42,7 +42,9 @@ else
     exit 1
 fi
 
-if command -v gstat >/dev/null 2>&1; then
+if stat -L -c %s "$FIRMWARE/root-patched.squashfs" >/dev/null 2>&1; then
+    SIZE=$(stat -L -c %s "$FIRMWARE/root-patched.squashfs")
+elif command -v gstat >/dev/null 2>&1; then
     SIZE=$(gstat -L -c %s "$FIRMWARE/root-patched.squashfs")
 else
     SIZE=$(stat -f %z "$FIRMWARE/root-patched.squashfs")
